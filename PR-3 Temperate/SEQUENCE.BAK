#include <stdio.h>
#include <conio.h>
#define pf printf
main()
{
	int i, j, n, num=1;
	clrscr();

	pf("Enter the limit of Loop: ");
	scanf("%d", &n);
	for (i=1; i<=n; i++)
	{
		pf("%d,", num);
		num = num * 10+1;
		pf("%d",num);

		if (i<n)
		{
		pf(",");
		}

	}
	 getch();
}

