#include<stdio.h>
#include<conio.h>
#define pf printf
#define cl clrscr

main()
{
float length, width, area;
cl();

//area of rectangle is length*width So,

pf("Enter a value for length of rectangle: ");
scanf("%f", &length);

pf("Enter the value for width of rectangle: ");
scanf("%f", &width);

area = length * width;

//Now it will display the area of rectangle

pf("The Area of Rectangle is : %.3f\n" , area);

	getch();
}