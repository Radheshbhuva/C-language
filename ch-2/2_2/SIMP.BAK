#include <stdio.h>
#include <conio.h>

#define cl clrscr
#define pf printf
#define sf scanf

main()
{
float principle, rate, time, simpleinterest;
float h = 100;
float rest;
cl();

	pf("Enter the value of principle : ");
	sf("%f", &principle);

	pf("Enter the value of Rate : ");
	sf("%f",&rate);

	pf("Enter the value of time : ");
	sf("%f", &time);

	rest = principle * rate * time;
	simpleinterest = rest/100;

	pf ("The simple interest is : %.2f\n ",simpleinterest);

	getch();

}